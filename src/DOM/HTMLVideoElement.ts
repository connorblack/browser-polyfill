// src/DOM/HTMLVideoElement.ts

import '../types'

import Element from './Element'

class HTMLVideoElement extends Element {
  constructor(tagName: string) {
    super(tagName)
  }
}

export default HTMLVideoElement
