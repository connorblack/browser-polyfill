// src/DOM/HTMLCanvasElement.ts

import '../types'

import Element from './Element'

class HTMLCanvasElement extends Element {
  constructor(tagName: string) {
    super(tagName)
  }
}

export default HTMLCanvasElement
